import { TagFormValidatorDirective } from './tag-form-validator.directive';

describe('TagFormValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new TagFormValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
