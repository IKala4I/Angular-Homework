import { TagBackgroundDirective } from './tag-background.directive';

describe('TagBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new TagBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
